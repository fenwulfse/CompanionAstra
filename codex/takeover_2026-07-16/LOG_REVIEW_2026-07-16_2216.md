# Astra Log Review

Reviewed: 2026-07-16 at approximately 10:16 PM EDT

Log: `C:\Users\fen\Documents\My Games\Fallout4\Logs\Script\Papyrus.0.log`

Session: 9:21:32 PM through 10:10:09 PM EDT

## Astra Results

- The awareness system initialized and remained active: 50 `[ASTRA_BRAIN]` entries.
- Dialogue logging worked: 83 `[ASTRADLG]` entries.
- The memoir progressed through three distinct chapters: stages 20, 30, and 40.
- Codsworth's quest called vanilla `DismissCompanion`/`SetCompanion` at 10:08 PM.
- Astra was available for recruitment again at 10:09 PM, consistent with a successful companion handoff.
- No missing Astra scripts or missing TIF fragment scripts were reported.
- The removed invalid `CompanionActor` property warning did not return.
- `CompanionCrimeFactionHostilityScript` did not return.
- No suspended stacks or stack dumps were recorded.

## Astra Warning Fixed After Review

`Vault111ExitMarkerRef` remained as an old VMAD fill on `MQAstraALTQuestScript`, although the current source no longer declares or uses it. It warned six times across loads/reverts. The property was removed from the plugin, strictly verified, and deployed after this log closed.

## Residual Red Flags

- The log contains 596 anonymous `[None].companionactorscript` stack frames, including 115 `None` calls and 207 `no native object bound` errors. They occur during global Followers events and are not identified as Astra's live actor `07000804`. They predate this patch, but remain worth isolating in a separate clean-session test.
- Creation Club and DLC scripts produce extensive property/type mismatch noise: 511 lines containing `error` and 1,112 containing `warning` overall. Most name `ccBGSFO4115`, `ccBGSFO4116`, DLC03, DLC04, DLC06, or VR Workshop scripts rather than Astra. This suggests installed plugin/loose-script version mismatch and makes the Papyrus log much noisier than healthy.
- The alternate MQ path logged `tunnel prime skipped (Stage85): MQ302Min fragment cast failed`. This is quest-side work and was not changed in the companion patch.

## Next Log Check

The next launch should confirm that `Vault111ExitMarkerRef` is gone. For the cleanest diagnostic, start Fallout 4 fresh, load only the intended test save once, perform one story request and one companion switch, then exit rather than loading or reverting several saves in the same session.
